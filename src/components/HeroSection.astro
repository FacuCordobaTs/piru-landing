---
---

<section class="relative min-h-screen flex items-center justify-center overflow-hidden">
  <!-- Desktop Background -->
  <div class="absolute inset-0 hidden md:block">
    <img
      src="epicdesktopbackground.jpg"
      alt="Fantasy landscape background"
      class="w-full h-full object-cover"
      loading="eager"
    />
  </div>

  <!-- Mobile Background -->
  <div class="absolute inset-0 md:hidden">
    <img
      src="epicmobilebackground.jpg"
      alt="Fantasy landscape background"
      class="w-full h-full object-cover"
      loading="eager"
    />
  </div>

  <!-- Overlay -->
  <div class="absolute inset-0 bg-black/50"></div>

  <!-- Content -->
  <div class="relative z-10 text-center text-white px-6 max-w-4xl mx-auto">

    <!-- Main Heading -->
    <h2 class="text-4xl md:text-6xl lg:text-7xl font-bold mb-6 leading-tight text-balance">
      Tu Camino a la
      <br />
      <span class="text-yellow-400">Mejor Versión</span>
      <br />
      de Ti Comienza Ahora
    </h2>

    <!-- Subtitle -->
    <p class="text-xl md:text-2xl mb-12 text-gray-200 max-w-2xl mx-auto text-balance">
      Combina hábitos, NoFap y progreso RPG para forjar tu leyenda
    </p>

    <!-- CTA Button -->
    <a
      href="#beta-signup"
      class="golden-button inline-block text-lg md:text-xl"
    >
      <span class="golden-button-text">Unirse a la Beta</span>
    </a>

    <p class="text-sm text-gray-300 mt-4">Disponible para Android • Prueba Cerrada</p>
  </div>
</section>
